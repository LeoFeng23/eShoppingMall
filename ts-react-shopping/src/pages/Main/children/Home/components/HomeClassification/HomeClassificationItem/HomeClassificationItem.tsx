// @flow
import * as React from 'react';
import './HomeClassificationItem.scss'

type Props = {
    picUrl: string,
    text: string
};
export const HomeClassificationItem = (props: Props) => {
    return (
        <div className="home-classification-item">

        </div>
    );
};
